# Docs — 项目文档目录

> 每次提交代码后检查本文件是否需要同步更新目录结构。

## 目录结构

```
docs/
  flows/              ← 指令流程图（Mermaid markdown），供 README 引用
  README.md           ← 本文件
```

所有流水线产物文档已迁移到 `.jarvis/YYYY-MM-DD/` 目录（本地开发用，不提交仓库）。
