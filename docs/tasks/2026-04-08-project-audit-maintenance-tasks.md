# 任务草案：2026-04-08 项目审查与维护

## 任务 ID: AUDIT-001 | 架构与维护性审查输出
- 类型: 审查与分析
- 优先级: 高
- 完成标准: 按严重级别整理出服务端、前端与共享包的审查结论，明确区分阻塞问题、建议修改项、参考优化项并附上至少一处可跟进的高置信度发现；结果以可共享的 Markdown 记录，方便后续工作引用。
- test_strategy: 通过逐目录、逐模块静态走查，重点覆盖 `apps/server`、`apps/web`、`apps/admin`、`packages/http-client`、`packages/schemas`、`packages/shared`，并复现关键控制流 / 契约点，确认结论可靠。
- 风险与文件所有权提醒: 关注 `apps/server` 入口与 OpenAPI 组织、`apps/web` / `apps/admin` 的路由鉴权与构建配置、`packages/*` 中共享契约代码。因文件体积较大且多人共用，避免一次性改动过多模块；复杂结构性问题留待后续迭代。

## 任务 ID: AUDIT-002 | 高置信度低风险问题修复
- 类型: 工程修复
- 优先级: 高
- 完成标准: 基于审查结论独立执行一轮至少 2-3 个高置信度、低风险的可验证修复，每项修复都说明为何属于“可直接修复”，并列出回归验证步骤。
- test_strategy: 针对每个修复点执行最小范围的 Vitest / 单元测试或 Bun 命令，并确认 `bun run lint`、`bun run typecheck` 覆盖到的路径没有回退。
- 风险与文件所有权提醒: 修复集中在 `apps/web`、`apps/admin`、`apps/server` 已识别的高置信度位置，避免无计划地跨包扩散；如触及共享契约，必须明确唯一责任方。

## 任务 ID: AUDIT-003 | apps/web 自动化测试补充
- 类型: 测试
- 优先级: 高
- 完成标准: 在 `apps/web` 中为本轮修复点或高风险用户链路新增 Vitest 自动化测试，至少覆盖一个关键场景或一条易回归逻辑；测试可在不引入 DOM-heavy 依赖的前提下运行。
- test_strategy: 利用现有 Vitest + Bun 运行，编写纯函数、路由辅助或 state-flow 测试；必要时补充轻量测试辅助函数，不引入新的重型测试栈。
- 风险与文件所有权提醒: 当前前端测试设施主要是 Vitest，优先采用逻辑 / 路由辅助层测试；避免引入新框架或大规模 fixture。

## 任务 ID: AUDIT-004 | 运行并记录关键验证命令
- 类型: 验证 / 质量控制
- 优先级: 中
- 完成标准: 在本轮审查与修复完成后依次运行 `bun run lint`、`bun run typecheck`、`bun run test`、`bun run build`，并记录每个命令是否通过及遇到的异常。
- test_strategy: 所有命令在当前仓库根目录运行，结果需能关联回具体改动。
- 风险与文件所有权提醒: 这些命令会触及全仓库，运行前确认当前工作区修改范围清晰；若发现失败，需要明确归因到对应目录或模块。
