# 工程化二期任务分解文档

> 需求文档: [docs/requirements/2026-05-08-工程化二期.md](../requirements/2026-05-08-工程化二期.md)
> 日期: 2026-05-08
> 状态: 已分解，待规划

---

## 任务概览

| TASK | REQ | 名称 | 类型 | 优先级 | 规模 | 风险 |
|------|-----|------|------|--------|------|------|
| TASK-001 | REQ-001 | TypeScript 基础设施搭建 | 直接开发 | P0 | M (~100行) | 中 |
| TASK-002 | REQ-001 | ESLint TypeScript 适配 | 直接开发 | P0 | S (~50行) | 低 |
| TASK-003 | REQ-001 | 引擎核心模块 TS 迁移 | 直接开发 | P0 | L (~200行) | 中 |
| TASK-004 | REQ-001 | 服务端+CLI+测试 TS 迁移 | 直接开发 | P0 | L (~300行) | 中 |
| TASK-005 | REQ-002 | GitHub Actions CI 流水线 | 直接开发 | P0 | M (~120行) | 低 |
| TASK-006 | REQ-002 | Husky Git Hooks | 直接开发 | P0 | S (~50行) | 低 |
| TASK-007 | REQ-002, REQ-004 | Release 流程重写+脚本清理 | 直接开发 | P0 | M (~150行) | 中 |
| TASK-008 | REQ-003 | Agent Effort 统一 max→high | 直接开发 | P1 | S (~50行) | 低 |
| TASK-009 | REQ-004 | .env Token 清理 | 直接开发 | P0 | XS (~30行) | 低 |
| TASK-010 | REQ-005 | 临时文件目录约束 | 直接开发 | P1 | S (~40行) | 低 |
| TASK-011 | REQ-006 | 会话断联文档确认 | 直接开发 | P2 | XS (~20行) | 低 |
| TASK-012 | REQ-007 | 版本发布流程文档化 | 直接开发 | P1 | S (~60行) | 低 |

**总计**: 12 个任务，预估总变更 ~1170 行。分两轮交付：第一轮（TASK-001~007,009）约 1000 行，第二轮（TASK-008,010~012）约 170 行。

---

## 需求追溯矩阵

| REQ | 映射 TASK | 覆盖状态 |
|-----|-----------|---------|
| REQ-001 | TASK-001, TASK-002, TASK-003, TASK-004 | 全覆盖 |
| REQ-002 | TASK-005, TASK-006, TASK-007 | 全覆盖 |
| REQ-003 | TASK-008 | 全覆盖 |
| REQ-004 | TASK-007, TASK-009 | 全覆盖 |
| REQ-005 | TASK-010 | 全覆盖 |
| REQ-006 | TASK-011 | 全覆盖 |
| REQ-007 | TASK-012 | 全覆盖 |

---

## 任务详细分解

### TASK-001: TypeScript 基础设施搭建

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-001 |
| 任务类型 | 直接开发 |
| 优先级 | P0 |
| 预估变更 | M (~100行) |
| 风险等级 | 中（共享区域：package.json） |
| test_strategy | manual_only（配置验证，无业务逻辑） |
| 依赖 | 无 |
| 被依赖 | TASK-002, TASK-003, TASK-004, TASK-005, TASK-006 |

**描述**: 搭建 TypeScript 编译基础设施，使项目从 JS+JSDoc 切换到 TypeScript 严格模式。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `package.json` | 更新 scripts(typecheck/build)、bin(dist/)、files(dist/)、devDependencies | **TASK-001 独占** |
| `jsconfig.json` → `tsconfig.json` | 替换：strict 模式、module=Preserve、declaration、outDir dist/ | **新文件** |
| `vitest.config.js` → `vitest.config.ts` | 重命名，更新 include 匹配 .ts | **TASK-001 独占** |
| `types/punycode.d.ts` | 保留，适配 tsconfig paths | 无需改动 |

**详细变更**:

1. **tsconfig.json**（替换 jsconfig.json）:
   - `strict: true`（替代 `checkJs/allowJs` 的弱类型检查）
   - `module: "Preserve"`（ESM 项目不转换模块语法）
   - `moduleResolution: "bundler"`
   - `declaration: true`, `declarationMap: true`
   - `outDir: "dist"`, `rootDir: "."`
   - `include: ["src/**/*.ts", "tests/**/*.ts"]`
   - `exclude: ["node_modules", "dist", ".claude", "src/templates"]`
   - 保留 `paths: { "punycode": ["./types/punycode.d.ts"] }`

2. **package.json** 变更（TASK-001 是唯一所有者，处理所有结构改动）:
   - `scripts.typecheck`: `tsc --project tsconfig.json --noEmit`（开发期类型检查）
   - `scripts.build`: `tsc --project tsconfig.json`（编译产出 dist/）
   - `scripts.prepare`: `"husky"`（预留给 TASK-006）
   - `bin`: `{ "jarvis": "dist/bin/jarvis.js", "jaf": "dist/bin/jarvis.js" }`
   - `files`: `["dist/"]`
   - `devDependencies`: 添加 `typescript@^6.0`（当前已有 6.0.3, 补全）、`husky@^9`, `lint-staged@^15`（Husky 预装）、`@types/eslint__js@^10`（如需要）

3. **vitest.config.ts**: 重命名文件，将 `include: ['tests/**/*.test.js']` 改为 `['tests/**/*.test.ts']`

**完成标准**:
- [ ] `tsconfig.json` 存在且为 strict 模式
- [ ] `npm run typecheck` 可运行（无文件时的空检查通过）
- [ ] `npm run build` 可产出 `dist/` 目录
- [ ] `vitest.config.ts` 配置正确
- [ ] 新增 devDependencies 已安装（`npm install` 无报错）

---

### TASK-002: ESLint TypeScript 适配

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-001 |
| 任务类型 | 直接开发 |
| 优先级 | P0 |
| 预估变更 | S (~50行) |
| 风险等级 | 低 |
| test_strategy | manual_only（ESLint 配置验证） |
| 依赖 | TASK-001（tsconfig 就位 + @typescript-eslint 已安装） |
| 被依赖 | TASK-003, TASK-004 |

**描述**: 将当前纯 JS 的 ESLint flat config 适配为支持 TypeScript。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `eslint.config.js` | 添加 @typescript-eslint 插件和解析器 | **TASK-002 独占** |

**详细变更**:
- 引入 `typescript-eslint` 的 `config()` helper
- 将现有 `js.configs.recommended` 替换为或追加 `tseslint.configs.recommended`
- `languageOptions.parser` 设为 `@typescript-eslint/parser`
- `rules` 中移除 JSDoc-only 的规则（`no-unused-vars` 改为 `@typescript-eslint/no-unused-vars`）
- `ignores` 中调整：移除 `*.config.js` 通配（vitest.config 已改 .ts），添加 `dist/`
- `files` 范围调整为 `['src/**/*.ts', 'tests/**/*.ts']`

**完成标准**:
- [ ] ESLint 不再对 .ts 文件报解析错误
- [ ] `npm run lint` 对现有 .ts 文件运行正常（即使暂时有 warn/error）

---

### TASK-003: 引擎核心模块 TS 迁移

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-001 |
| 任务类型 | 直接开发（核心引擎逻辑，已有一期测试覆盖） |
| 优先级 | P0 |
| 预估变更 | L (~200行修改) |
| 风险等级 | 中（跨 5 个文件的重命名 + 类型标注） |
| test_strategy | test_after（现有 `tests/db.test.js` 和 `tests/gates.test.js` 覆盖相关逻辑，迁移后验证通过） |
| 依赖 | TASK-001（tsconfig）, TASK-002（ESLint TS） |
| 被依赖 | TASK-004（server.ts 导入这些模块） |

**描述**: 将引擎核心模块（数据库、流水线 Gate 定义、Agent 注册、文件同步）从 JS+JSDoc 迁移到 TypeScript。这些是项目的数据和业务逻辑基础层，不依赖其他 src 模块。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `src/engine/db.js` → `src/engine/db.ts` | 重命名，JSDoc→TS 类型 | **TASK-003 独占** |
| `src/engine/gates.js` → `src/engine/gates.ts` | 重命名，JSDoc→TS 类型 | **TASK-003 独占** |
| `src/engine/agent-registry.js` → `src/engine/agent-registry.ts` | 重命名，JSDoc→TS 类型 | **TASK-003 独占** |
| `src/engine/agent-fs.js` → `src/engine/agent-fs.ts` | 重命名，JSDoc→TS 类型 | **TASK-003 独占** |
| `src/web/routes.js` → `src/web/routes.ts` | 重命名，JSDoc→TS 类型 | **TASK-003 独占** |

**详细变更**:

1. **db.ts** (~283 行源码):
   - 移除所有 `@param`/`@returns` JSDoc 类型注解
   - 添加函数参数和返回值的 TypeScript 类型
   - `DatabaseSync` 类型从 `node:sqlite` 导入已有类型
   - 保留迁移逻辑（已有向后兼容代码，不变）

2. **gates.ts** (~118 行源码):
   - 移除 JSDoc 类型注解
   - 添加 TypeScript 类型：`PIPELINE_DEFS` 需完整接口定义
   - `GATE_OPERATIONS` 的 `allow`/`deny` 需字面量联合类型

3. **agent-registry.ts** (~227 行源码):
   - 移除 JSDoc 类型注解
   - 定义 `AgentInfo`、`AgentFileMap`、`PlatformConfig` 接口
   - `_agentList` 和 `_agentFiles` 添加显式类型

4. **agent-fs.ts** (~101 行源码):
   - 移除 JSDoc 类型注解
   - `syncAgentFile` 函数添加完整参数/返回值类型

5. **routes.ts** (~302 行源码):
   - 移除 JSDoc 类型注解
   - Hono `Context` 类型从 `hono` 导入
   - API 响应对象添加类型

**风险说明**: 此任务涉及 5 个文件约 1000 行源码的改名 + 类型标注，但因是机械性改动（移除 JSDoc + 添加 TS 类型），不涉及逻辑变更，风险可控。选择 L 规模而非拆分，因为这些模块共同构成引擎"底层"，拆分会导致中间状态无法通过 typecheck。

**完成标准**:
- [ ] 5 个 `.js` 文件删除，对应 `.ts` 文件存在
- [ ] `npm run typecheck` 对这些文件 0 error
- [ ] `npm run lint` 对这些文件 0 error
- [ ] 现有测试（`tests/db.test.js`, `tests/gates.test.js`）迁移后仍然通过（将在 TASK-004 中统一迁移测试文件）

---

### TASK-004: 服务端+CLI+测试 TS 迁移

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-001 |
| 任务类型 | 直接开发 |
| 优先级 | P0 |
| 预估变更 | L (~300行修改) |
| 风险等级 | 中（跨 8 个文件，依赖 TASK-003 产出） |
| test_strategy | test_after（迁移后运行全量测试） |
| 依赖 | TASK-003（导入 engine/*.ts 和 web/routes.ts） |
| 被依赖 | 无（TS 迁移最后一步） |

**描述**: 将服务端入口、CLI 入口、安装逻辑、测试文件从 JS 迁移到 TypeScript。这些模块依赖 TASK-003 产出的引擎核心模块。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `src/engine/server.js` → `src/engine/server.ts` | 重命名，JSDoc→TS 类型 | **TASK-004 独占** |
| `src/cli.js` → `src/cli.ts` | 重命名，JSDoc→TS 类型 | **TASK-004 独占** |
| `src/install.js` → `src/install.ts` | 重命名，JSDoc→TS 类型 | **TASK-004 独占** |
| `src/hook.js` → `src/hook.ts` | 重命名，JSDoc→TS 类型 | **TASK-004 独占** |
| `src/doctor.js` → `src/doctor.ts` | 重命名，JSDoc→TS 类型 | **TASK-004 独占** |
| `tests/gates.test.js` → `tests/gates.test.ts` | 重命名，import 路径适配 | **TASK-004 独占** |
| `tests/db.test.js` → `tests/db.test.ts` | 重命名，import 路径适配 | **TASK-004 独占** |
| `bin/jarvis.js` | 更新 import 路径指向 `dist/src/cli.js` | **TASK-004 独占** |

**详细变更**:

1. **server.ts** (~636 行源码):
   - 移除所有 JSDoc 类型注解
   - Hono `Context`、MCP SDK 类型使用已有类型定义
   - `registerMcpTools` 的 Zod schema 不变（Zod 自带类型推导）
   - `startEngine`/`startWeb`/`stopEngine`/`engineStatus` 添加完整类型

2. **cli.ts** (~358 行源码):
   - 移除 JSDoc 类型注解
   - `parseArgs`、`resolveScope`、`diffPlatform` 等辅助函数添加类型
   - `PLATFORMS` 常量添加类型

3. **install.ts** (~284 行源码):
   - 移除 JSDoc 类型注解
   - `install`、`mergeDir`、`installMcp` 等函数添加类型

4. **hook.ts** (~66 行源码):
   - 移除 JSDoc 类型注解
   - `api`、`hookCommand` 添加类型

5. **doctor.ts** (~84 行源码):
   - 移除 JSDoc 类型注解
   - `doctor` 函数添加类型

6. **测试文件迁移**:
   - `tests/gates.test.ts`、`tests/db.test.ts` 重命名
   - import 路径从 `'../src/engine/xxx.js'` 改为 `'../src/engine/xxx.js'`（ESM 保持 .js 扩展名，tsc 会解析为 .ts）

7. **bin/jarvis.js**: import 从 `'../src/cli.js'` 改为 `'../dist/src/cli.js'`（编译后入口）

**实现注意**: ESM TypeScript 项目中，import 路径保持 `.js` 扩展名（TypeScript 编译器在类型检查时自动解析对应的 `.ts` 文件，但运行时代码使用编译后的 `.js`）。不需要在 import 中写 `.ts`。

**风险说明**: 此任务涉及 8 个文件约 1600+ 行源码，但与 TASK-003 一样是机械性改动（JSDoc → TS 类型），不涉及逻辑变更。选择 L 规模而非拆分，因为这些模块共同依赖 TASK-003 的产出，且完成标准是 `npm run typecheck` 全量通过——中间状态无法满足此条件。

**完成标准**:
- [ ] 所有 6 个源码 `.js` 文件删除，对应 `.ts` 文件存在
- [ ] 2 个测试 `.js` 文件删除，对应 `.ts` 文件存在
- [ ] `bin/jarvis.js` import 指向正确
- [ ] `npm run typecheck` strict 模式下 **0 error**（全量通过）
- [ ] `npm run lint` **0 error**
- [ ] `npm run test` **全部 18 测试通过**（断言无变化，仅路径适配）
- [ ] `npm run build` 可成功编译并产出 `dist/` 目录

---

### TASK-005: GitHub Actions CI 流水线

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-002 |
| 任务类型 | 直接开发 |
| 优先级 | P0 |
| 预估变更 | M (~120行) |
| 风险等级 | 低 |
| test_strategy | manual_only（CI YAML 配置，需 push 后验证） |
| 依赖 | TASK-001（依赖 devDependencies 中的 TypeScript/ESLint 配置） |
| 被依赖 | 无 |

**描述**: 新建 `.github/workflows/ci.yml`，实现 push 时自动 lint + typecheck + test，tag push 时 build + release + publish。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `.github/workflows/ci.yml` | **新建** | **TASK-005 独占** |

**详细变更**:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.x]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          registry-url: 'https://registry.npmjs.org'
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test

  publish:
    needs: check
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: 'https://registry.npmjs.org'
      - run: npm ci
      - run: npm run build
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create ${{ github.ref_name }} \
            --title "${{ github.ref_name }}" \
            --generate-notes
      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish
```

**完成标准**:
- [ ] `.github/workflows/ci.yml` 文件存在
- [ ] push 到 main 时自动触发 check job
- [ ] v* tag push 时自动触发 publish job（build + GitHub Release + npm publish）
- [ ] workflow 中无 token 硬编码，全部通过 GitHub Secrets

---

### TASK-006: Husky Git Hooks

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-002 |
| 任务类型 | 直接开发 |
| 优先级 | P0 |
| 预估变更 | S (~50行) |
| 风险等级 | 低 |
| test_strategy | manual_only（git hooks 本地验证） |
| 依赖 | TASK-001（package.json 中已添加 `prepare: "husky"` 和 husky/lint-staged devDeps） |
| 被依赖 | 无 |

**描述**: 配置 Husky v9 + lint-staged，实现 pre-commit 检查暂存文件和 pre-push 运行全量 check。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `.husky/pre-commit` | **新建** | **TASK-006 独占** |
| `.husky/pre-push` | **新建** | **TASK-006 独占** |
| `package.json` | 添加 `lint-staged` 配置项 | 需与 TASK-001 协调（仅添加配置块） |

**详细变更**:

1. **初始化 Husky**:
   ```bash
   npx husky init
   ```
   这会创建 `.husky/` 目录和 `pre-commit` 文件，同时触发 `prepare` 脚本写入 `_/husky.sh`。

2. **`.husky/pre-commit`**:
   ```bash
   npx lint-staged
   ```

3. **`.husky/pre-push`**:
   ```bash
   npm run check
   ```

4. **`package.json` 的 `lint-staged` 配置**（追加，非覆盖）:
   ```json
   "lint-staged": {
     "*.ts": [
       "eslint --fix",
       "prettier --write"
     ]
   }
   ```

**共享区域冲突处理**: `package.json` 的 `lint-staged` 配置项由 TASK-006 添加。由于 TASK-001 已声明独占 `package.json`，采用如下方案：**TASK-006 在 TASK-001 完成后向 package.json 增量添加 `lint-staged` 字段，不触碰 TASK-001 已写入的部分。**

**完成标准**:
- [ ] `.husky/` 目录存在且包含 `pre-commit` 和 `pre-push`
- [ ] `git commit` 时自动运行 lint-staged
- [ ] `git push` 前自动运行 `npm run check`
- [ ] `package.json` 中 `lint-staged` 配置存在

---

### TASK-007: Release 流程重写+脚本清理

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-002（发布自动化）, REQ-004（Gitee 脚本删除 + token 清理） |
| 任务类型 | 直接开发 |
| 优先级 | P0 |
| 预估变更 | M (~150行) |
| 风险等级 | 中（改动 release 流程，影响发布） |
| test_strategy | manual_only（发布脚本需实际发布来验证，建议用 `--dry-run` 或预发布版本测试） |
| 依赖 | TASK-001（package.json 版本号在 TS 迁移中锁定） |
| 被依赖 | 无 |

**描述**: 重写 release 脚本脱离 Gitee 依赖和 token 环境变量，改用 `gh` CLI；删除 Gitee 同步脚本；更新 GitHub Actions release workflow。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `scripts/release.sh` | **重写**：删除 Gitee 部分，用 `gh release create` 替代 curl | **TASK-007 独占** |
| `scripts/sync-github-releases.js` | **重写**：改用 `gh release create` CLI | **TASK-007 独占** |
| `scripts/sync-gitee-releases.js` | **删除** | **TASK-007 独占** |
| `.github/workflows/release.yml` | **更新**：改 GitHub URL + 用 `gh` CLI | **TASK-007 独占** |

**详细变更**:

1. **`scripts/release.sh`** 重写要点:
   - 步骤 1/3: 更新 package.json 版本号（不变）
   - 步骤 2/3: `git tag` + `git push origin main && git push origin v<VERSION>`（只推 GitHub origin）
   - 步骤 3/3: `npm publish`（只依赖 `~/.npmrc`，移除 `$NPM_TOKEN` 环境变量检查）
   - **删除**: Gitee Release API curl 步骤（原步骤 4/5）
   - **删除**: GitHub Release curl 步骤（原步骤 5/5），改为本地 `gh release create`

2. **`scripts/sync-github-releases.js`** 重写:
   - 不再依赖 `GITHUB_TOKEN` 环境变量
   - 改用 `gh release list` + `gh release create` CLI 命令
   - 保留 `--dry-run` 模式

3. **`scripts/sync-gitee-releases.js`**: 直接删除文件。

4. **`.github/workflows/release.yml`** 更新:
   - 移除 Gitee URL 引用
   - changelog link 改 GitHub URL
   - 用 `gh release create` 替代 `softprops/action-gh-release`

**完成标准**:
- [ ] `scripts/sync-gitee-releases.js` **已删除**（文件不存在）
- [ ] `scripts/release.sh` 使用 `gh release create`，不含 Gitee/Token 引用
- [ ] `scripts/release.sh` npm publish 不检查 `$NPM_TOKEN`（本地依赖 `~/.npmrc`）
- [ ] `scripts/sync-github-releases.js` 使用 `gh` CLI
- [ ] `.github/workflows/release.yml` 引用 GitHub URL
- [ ] `grep GITEE` 在 `scripts/` 中无匹配

---

### TASK-008: Agent Effort 统一 max→high

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-003 |
| 任务类型 | 直接开发 |
| 优先级 | P1 |
| 预估变更 | S (~50行) |
| 风险等级 | 低 |
| test_strategy | manual_only（grep 验证，无需测试框架） |
| 依赖 | 无 |
| 被依赖 | 无 |

**描述**: 将所有 Claude 平台 agent 模板中 `effort: max` 或 `effort: "max"` 统一改为 `effort: high`。纯文本替换。

**涉及文件**:
| 路径模式 | 操作 | 所有权 |
|----------|------|--------|
| `src/templates/platforms/claude/agents/*.md` | `effort: max` → `effort: high` | **TASK-008 独占** |
| `src/templates/platforms/claude/skills/*/SKILL.md` | `effort: max` → `effort: high`（如有） | **TASK-008 独占** |

**注意**: 这些模板文件在 TASK-003/004 的 TS 迁移中已通过 `eslint ignores` 排除，不受 TS 迁移影响。可以完全独立执行。

**完成标准**:
- [ ] `grep -r "effort.*max" src/templates/platforms/claude/` 无匹配结果
- [ ] 所有 agent 模板中 effort 值均为 `high` 或保持原值（如 `medium`、`low`）

---

### TASK-009: .env Token 清理

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-004 |
| 任务类型 | 直接开发 |
| 优先级 | P0 |
| 预估变更 | XS (~30行) |
| 风险等级 | 低 |
| test_strategy | manual_only（grep 验证） |
| 依赖 | 无 |
| 被依赖 | 无 |

**描述**: 删除含真实 token 的 `.env` 文件，清理 `.env.example` 中所有 token 占位符段落。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `.env` | **删除**（含真实 GITEE_TOKEN/GITHUB_TOKEN/NPM_TOKEN） | **TASK-009 独占** |
| `.env.example` | **精简**：只保留 `PORT=` 注释，删除所有 token 段 | **TASK-009 独占** |

**关键确认**: `.env` 已被 `.gitignore` 排除（`echo '.env' >> .gitignore` 已存在），不处于 git 追踪中。但文件在本地存在并包含真实 token——**必须删除并确认轮换已泄露的 token**。

**完成标准**:
- [ ] `.env` 文件不存在
- [ ] `.env.example` 仅含 `# PORT=3456` 注释，无 token 相关行
- [ ] `grep "GITEE_TOKEN|GITHUB_TOKEN|NPM_TOKEN" .env.example` 无匹配
- [ ] GitHub 个人设置中已撤销泄露的 token 并重新生成（安全操作，手动执行）

---

### TASK-010: 临时文件目录约束

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-005 |
| 任务类型 | 直接开发 |
| 优先级 | P1 |
| 预估变更 | S (~40行) |
| 风险等级 | 低 |
| test_strategy | manual_only |
| 依赖 | 无 |
| 被依赖 | TASK-012（共享 AGENTS.md，需串行） |

**描述**: 创建 `docs/tmp/` 目录，在 AGENTS.md 和模板全局指引中添加临时文件约束规则。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `docs/tmp/.gitkeep` | **新建** | **TASK-010 独占** |
| `AGENTS.md` | **追加**：临时文件目录约束规则 | 与 TASK-012 共享（串行） |
| `.gitignore` | **确认**：`docs/tmp/*` 规则已存在 | 只读确认，无需修改 |

**详细变更**:

1. 创建 `docs/tmp/` 目录并放入 `.gitkeep`
2. 在 AGENTS.md 的关键约束部分追加一条:
   ```markdown
   13. **临时文件统一目录** — 所有 agent 子任务产出的临时文件必须放在 `docs/tmp/`，禁止散落在项目根目录或其他位置。`docs/tmp/*` 已被 `.gitignore` 排除。
   ```
3. 在各平台 agent 模板的全局约束中同步加入此规则（如 `src/templates/platforms/claude/agents/planner.md` 等编排类 agent）

**完成标准**:
- [ ] `docs/tmp/.gitkeep` 存在
- [ ] `AGENTS.md` 含临时文件目录规则
- [ ] `.gitignore` 含 `docs/tmp/*` 排除规则（当前已存在，确认即可）

---

### TASK-011: 会话断联文档确认

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-006 |
| 任务类型 | 直接开发 |
| 优先级 | P2 |
| 预估变更 | XS (~20行) |
| 风险等级 | 低 |
| test_strategy | manual_only |
| 依赖 | 无 |
| 被依赖 | 无 |

**描述**: 确认断联重连机制现状，在 README 或文档中说明会话状态保持能力。**无需代码改动。**

**变更**: 
- 在 `README.md` 或 `CLAUDE.md` 中补充"断联恢复"说明段落（可选，已知即可）
- 当前状态已满足需求：sessions 不物理删除，2 小时无心跳标记 `inactive`，`session_join` 带 `resume_session_id` 可恢复

**完成标准**:
- [ ] 文档中存在断联/重连机制的说明（README.md 或 CLAUDE.md 的引擎章节）
- [ ] 或：确认无需文档化，关闭此需求

---

### TASK-012: 版本发布流程文档化

| 属性 | 值 |
|------|-----|
| 映射需求 | REQ-007 |
| 任务类型 | 直接开发 |
| 优先级 | P1 |
| 预估变更 | S (~60行) |
| 风险等级 | 低 |
| test_strategy | manual_only |
| 依赖 | TASK-007（release.sh 已重写）, TASK-010（AGENTS.md 已更新，需协调） |
| 被依赖 | 无 |

**描述**: 将版本管理和发布流程写入项目文档，替换 AGENTS.md 中已过时的"双远程推送"发布流程，新增 GitHub Actions workflow 触发说明。

**涉及文件**:
| 文件 | 操作 | 所有权 |
|------|------|--------|
| `AGENTS.md` | **重写**发布流程章节（当前 60-117 行） | 与 TASK-010 共享（串行，在 TASK-010 之后执行） |

**详细变更**:

AGENTS.md 中的"发布流程"章节（当前第 60-117 行）全部替换为：
1. **版本号规则**: MAJOR.MINOR.PATCH 语义（patch=bug fix, minor=new feature, major=breaking change）
2. **本地发布**: `bash scripts/release.sh <version>` 一键发布（依赖 `~/.npmrc`）
3. **CI 自动发布**: push `v*` tag 到 GitHub → GitHub Actions 自动 build + GitHub Release + npm publish（通过 `NPM_TOKEN` Secret）
4. **验证清单**: npm version view + GitHub Release URL 确认

**共享区域冲突**: AGENTS.md 同时被 TASK-010 和 TASK-012 修改。**TASK-010 先执行**（追加约束条目），**TASK-012 后执行**（重写发布流程章节，并确认 TASK-010 追加的条目未被覆盖）。

**完成标准**:
- [ ] AGENTS.md 发布流程章节已更新为单 remote（GitHub）模式
- [ ] 文档说明版本号规则（MAJOR.MINOR.PATCH）
- [ ] `release.sh` 简化后仍可一键发布（本地 `npm login` 后）
- [ ] GitHub Actions release workflow 可通过 tag push 触发

---

## DDD 分类

**本次无 DDD 任务。** 理由如下：

- TS 迁移（REQ-001）：代码风格转换，无业务规则建模
- CI/CD 流水线（REQ-002）：纯配置/自动化，无领域逻辑
- Effort 统一（REQ-003）：纯文本替换
- Token/脚本清理（REQ-004）：删除 + 脚本重写，无领域聚合
- 临时文件约束（REQ-005）：文档规则新增
- 会话断联（REQ-006）：确认项，无代码改动
- 版本发布文档化（REQ-007）：文档编写

所有任务均不涉及状态机、聚合根、领域服务等 DDD 战术模式。

---

## TDD 与直接开发分类

### TDD 任务

**本次无 TDD 任务。** 理由如下：

- TS 迁移（REQ-001）：类型系统的正确性由 `tsc --strict` 编译器保证，无需测试验证类型标注
- CI/CD（REQ-002）：不能对 CI YAML 配置写单元测试
- Effort 统一（REQ-003）：grep 足以验证
- Token 清理（REQ-004）：grep 足以验证
- 文档类任务（REQ-005/006/007）：无测试对象

> 现有 18 个单元测试（`tests/db.test.js`, `tests/gates.test.js`）在 TS 迁移后仍保持断言逻辑不变，仅 import 路径适配。这些测试已在 TASK-004 中被迁移和验证。

### 直接开发任务

全部 12 个任务均归类为直接开发。

---

## 风险任务

| TASK | 风险等级 | 风险描述 | 不拆分理由 |
|------|---------|---------|-----------|
| TASK-001 | 中 | 独占修改 `package.json`（bin/files/scripts/devDeps），改动影响全局入口 | - |
| TASK-003 | 中 | 跨 5 个文件，引擎核心模块互相依赖，中间状态无法通过 typecheck | 机械性改动（JSDoc→TS 类型），非新逻辑。拆分导致 typecheck 断裂面增大 |
| TASK-004 | 中 | 跨 8 个文件，约 1600+ 行源码，依赖 TASK-003 产出 | 同上，机械性改动。拆分导致中间状态 typecheck 无法通过 |
| TASK-007 | 中 | 改动 release 流程脚本，影响发布环节 | 文件集独立（scripts/ + .github/），建议用预发布版本验证 |

---

## 文件所有权与共享路径

### 独占文件（无冲突）

| 文件 | 所有者 | 说明 |
|------|--------|------|
| `tsconfig.json` | TASK-001 | 新建，替换 jsconfig.json |
| `eslint.config.js` | TASK-002 | 唯一修改者 |
| `vitest.config.ts` | TASK-001 | 重命名自 vitest.config.js |
| `types/punycode.d.ts` | TASK-001 | 保留，适配 tsconfig paths |
| `src/engine/db.ts` | TASK-003 | 引擎核心 |
| `src/engine/gates.ts` | TASK-003 | 引擎核心 |
| `src/engine/agent-registry.ts` | TASK-003 | 引擎核心 |
| `src/engine/agent-fs.ts` | TASK-003 | 引擎核心 |
| `src/web/routes.ts` | TASK-003 | Web 路由 |
| `src/engine/server.ts` | TASK-004 | 服务端入口 |
| `src/cli.ts` | TASK-004 | CLI 入口 |
| `src/install.ts` | TASK-004 | 安装逻辑 |
| `src/hook.ts` | TASK-004 | Hook 命令 |
| `src/doctor.ts` | TASK-004 | 健康检查 |
| `tests/gates.test.ts` | TASK-004 | 测试迁移 |
| `tests/db.test.ts` | TASK-004 | 测试迁移 |
| `bin/jarvis.js` | TASK-004 | 入口更新 |
| `.github/workflows/ci.yml` | TASK-005 | 新建 |
| `.github/workflows/release.yml` | TASK-007 | 更新 |
| `.husky/` | TASK-006 | 新建 |
| `scripts/release.sh` | TASK-007 | 重写 |
| `scripts/sync-github-releases.js` | TASK-007 | 重写 |
| `src/templates/platforms/claude/agents/*.md` | TASK-008 | 模板文件替换 |
| `.env` | TASK-009 | 删除 |
| `.env.example` | TASK-009 | 精简 |
| `docs/tmp/.gitkeep` | TASK-010 | 新建 |
| `README.md` | TASK-011 | 可选补充 |

### 共享文件（需串行）

| 文件 | 修改者 | 串行策略 |
|------|--------|---------|
| `package.json` | TASK-001（主）, TASK-006（增量） | TASK-001 完成所有结构改动（scripts/bin/files/devDeps）。TASK-006 在之后仅追加 `lint-staged` 配置块，不覆盖已有字段 |
| `AGENTS.md` | TASK-010（先）, TASK-012（后） | TASK-010 追加约束条目不覆盖已有内容。TASK-012 重写发布流程章节，同时确认 TASK-010 追加的条目保留。**严格串行**：TASK-010 完成后才启动 TASK-012 |
| `.gitignore` | TASK-010 | 唯一修改者（确认已有 `docs/tmp/*` 规则） |

---

## 推荐交付顺序

```
第 1 轮（P0 核心，约 1000 行）：
  串行链：TASK-001 → TASK-002 → TASK-003 → TASK-004
  并行组：TASK-005 ∥ TASK-006 ∥ TASK-009 ∥ TASK-007
           ↑ 这些任务之间无共享文件冲突，可以在 TS 迁移完成后并行执行

第 2 轮（P1/P2 收尾，约 170 行）：
  并行组：TASK-008 ∥ TASK-011
  串行链：TASK-010 → TASK-012
```

### 可视化依赖图

```
TASK-001 (TS infra)
  ├─→ TASK-002 (ESLint)
  │     └─→ TASK-003 (engine core)
  │           └─→ TASK-004 (server+CLI+tests)
  │
  ├─→ TASK-005 (GH Actions CI) ──┐
  ├─→ TASK-006 (Husky hooks) ────┤ 可并行（文件互不冲突）
  ├─→ TASK-009 (.env cleanup) ───┤
  └─→ TASK-007 (release rewrite)─┘

TASK-008 (effort max→high)     ← 独立，随时可做
TASK-011 (session doc)         ← 独立，随时可做

TASK-010 (tmp dir constraint)  → TASK-012 (release docs)
                                   ↑ 共享 AGENTS.md，必须串行
```

---

## 推荐的下一步

1. **Planner** 读取本文档，产出执行计划（parallel_batches）
2. **实现顺序建议**: 先打通 TS 迁移链路（TASK-001→004），这条链路是最长串行路径，完成后其他任务可大规模并行
3. **风险注意**: TASK-009 删除 `.env` 前确认已轮换泄露的 GitHub/npm token（当前 `.env` 含真实 token 值）

---

## 验证清单（任务分解自身 QA）

- [x] 所有 REQ-XXX 都至少映射到 1 个 TASK（追溯矩阵已确认）
- [x] 任务使用垂直切片策略（每个 TS 迁移任务覆盖完整模块路径；CI/CD/清理/文档各自独立可验证）
- [x] 无水平切片（无"数据库全部列迁移""API 全部端点迁移"等模式）
- [x] 每个任务有明确的优先级和 test_strategy
- [x] 依赖关系已明确（无循环依赖）
- [x] 并行机会已识别（TASK-005/006/007/009 可并行）
- [x] 风险任务已标注（TASK-001/003/004/007）
- [x] 第 1 轮总变更约 1000 行，第 2 轮约 170 行，均在合理范围内
- [x] 共享区域已指定唯一责任方（package.json→TASK-001, AGENTS.md→串行）
- [x] 每个任务有可独立验证的完成标准
