# 上线检查清单：v3.26.0 — 会话列表卡片化布局

> **日期**：2026-05-08 | **发布类型**：MINOR | **风险等级**：低

## 变更摘要

前端纯视觉重构：侧边栏会话列表从单行改为 2 行卡片布局 + 键盘可访问性增强。无后端变更、无 API 变更、无数据库迁移。

## 上线检查清单

| # | 检查项 | 状态 |
|---|--------|------|
| 1 | Lint 通过 | ✅ 0 errors |
| 2 | Type-check 通过 | ✅ 0 errors |
| 3 | 构建通过 | ✅ exit 0 |
| 4 | 测试通过 | ✅ 24/24 passed |
| 5 | 视觉验证 | ✅ 2 行布局 + ⋮ 按钮均正确渲染 |
| 6 | 控制台无 JS 错误 | ✅ 仅预存 favicon.ico 404 |
| 7 | 回归验证 | ✅ 10/10 项通过 |
| 8 | 版本号递增 | ✅ 3.25.3 → 3.26.0 |
| 9 | 文档同步 | ✅ README/CHANGELOG/docs 已更新 |
| 10 | Tag 已推送 | ✅ v3.26.0 → GitHub |

## 安全审计

| 检查项 | 结论 |
|--------|------|
| 新增依赖 | 无 |
| 用户输入处理 | 无变更（模板字符串 XSS 防护沿用现有 `escapeHtml`） |
| API 端点 | 无变更 |
| 权限模型 | 无变更 |
| 敏感数据暴露 | 无新增 |

**安全结论**：纯前端 HTML/CSS 重构，不涉及安全边界变更。

## 回滚预案

回滚方式：`git revert 54e65cf`（单 commit），恢复 `pipeline.html` 和 `README.md/package.json` 到 v3.25.3 状态。

回滚影响：
- 会话列表恢复为旧版单行布局
- ⋮ 按钮恢复为 hover 显示 + 条件渲染
- Web 面板其他功能不受影响

回滚验证：
1. `git revert 54e65cf --no-edit`
2. `npm run lint && npm run typecheck && npm run test`
3. 启动 Web 面板确认会话列表恢复旧版
