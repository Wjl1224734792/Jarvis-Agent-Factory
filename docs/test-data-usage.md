# 飞加项目海量测试数据生成说明

## 数据规模概览

| 数据类型 | 数量 | 说明 |
|---------|------|------|
| 用户 | 50 个 | 1 管理员 + 49 普通用户 |
| 用户设置 | 30 个 | 随机偏好设置 |
| 飞行器分类 | 6 个 | 多旋翼、固定翼、eVTOL、直升机、公务机、VTOL |
| 品牌 | 20 个 | DJI、Autel、亿航、Joby、小鹏汇天等 |
| 飞行器型号 | 30 个 | 覆盖各品牌主流机型 |
| 内容分类 | 5 个 | 资讯、评测、航拍、技术、指南 |
| 文件记录 | 195 个 | 头像、文章图、动态图、视频、排行榜封面等 |
| 帖子 | 60 个 | 30 文章 + 30 动态 |
| 帖子评论 | 120 条 | 含回复关系 |
| 帖子互动 | 200 条 | 点赞、收藏、分享 |
| 帖子评论点赞 | 80 条 | |
| 飞行器评测 | 40 条 | 含 1-5 星评分 |
| 飞行器型号评论 | 50 条 | 含回复关系 |
| 飞行器型号互动 | 80 条 | 收藏、浏览、对比 |
| 排行榜 | 10 个 | 社区 + 官方 |
| 排行榜项目 | 50 个 | 含排名、评分 |
| 排行榜评论 | 20 条 | |
| 排行榜项目评分 | 100 条 | 1-5 星 |
| 排行榜项目评论 | 40 条 | 含回复关系 |
| 用户关注 | 80 条 | 随机关注关系 |
| 通知 | 100 条 | 关注、评论、回复、点赞等 |
| 会话 | 30 个 | Web + App |
| 飞行器提交 | 15 个 | 待审核、已批准、已拒绝 |
| 帖子举报 | 15 条 | |
| 帖子评论举报 | 10 条 | |
| 排行榜举报 | 5 条 | |
| 排行榜项目举报 | 5 条 | |
| 排行榜项目评论举报 | 5 条 | |
| 站点设置 | 1 个 | |

**总计：约 1,300+ 条数据库记录 + 195 个 MinIO 文件 + 多组 Redis 缓存**

## 前置条件

```bash
# 启动基础设施
cd docker/database && docker-compose up -d
cd docker/redis && docker-compose up -d
cd docker/storage && docker-compose up -d
```

## 运行

```bash
bun run packages/db/src/seed.test-data.ts
```

## 测试账号

| 角色 | 账号 | 密码 | 说明 |
|------|------|------|------|
| 管理员 | testadmin | TestAdmin#123 | 后台管理 |
| 普通用户 | 138 开头手机号 | 无（短信登录） | 49 个用户 |

## Redis 快速测试数据

| 类型 | Key | 值 |
|------|-----|-----|
| 图形验证码 | `captcha:test_captcha_001` | code: `TEST01` |
| 短信验证码 | `sms:13800138000` | code: `888888` |
| 注册令牌 | `reg:test_reg_001` | phone: `13900139000` |
| 热门圈子 | `feed:hot-circle` | 12 条数据 |
| 热门机型 | `feed:hot-models` | 10 条数据 |
| 热门排行 | `feed:hot-rankings` | 5 条数据 |
| Hero 媒体 | `feed:hero-media` | 5 条数据 |

## Playwright 浏览器自动化

根目录的浏览器自动化脚本会复用这套测试数据：

```bash
bun run test:e2e
bun run test:e2e:headed
```

默认测试身份与联调口径：

| 项目 | 值 | 说明 |
|------|----|------|
| Web 端手机号 | `13800138000` | 作为普通用户登录 |
| 图形验证码 | 动态请求 `/auth/captcha/challenge` | 用例运行时实时获取 |
| 短信验证码 | `888888` | 直接复用测试数据中预置验证码，避免频控影响 |

说明：

- `test:e2e` 会先执行 `infra:up` 和 `db:reset:test-data`，确保浏览器用例基于固定数据启动。
- 若你单独手动跑 Playwright，也建议先执行 `bun run db:reset:test-data`。
- Playwright 执行完成后，数据库仍会保留在 `test-data` 状态；如需继续运行依赖基础 seed 的脚本或测试，请先执行 `bun run db:reset:dev`。

## 注意事项

- 脚本会先 `TRUNCATE` 所有测试相关表，确保数据干净
- 每次运行重新生成所有数据
- MinIO bucket: `feijia-media`，前缀 `test/`
- 数据使用随机分布，每次运行内容略有不同
