# 2026-04-22 server 超时治理与前端拆包计划

## 执行顺序
1. 复现 server 失败，并验证是否由默认测试超时导致。
2. 调整 server 测试入口到合理阈值，复跑完整 `test`。
3. 并行推进 `apps/admin` / `apps/web` 的 manualChunks 优化。
4. 复跑 `build`，记录 chunk 变化与剩余风险。
5. 更新 review 文档。

## 风险控制
- server 主线只改测试入口/配置，不顺手改业务逻辑。
- 前端拆包优先改构建策略，避免和正在演进的业务页面互相冲突。
