# 工程化二期执行计划

> 需求文档: [docs/requirements/2026-05-08-工程化二期.md](../requirements/2026-05-08-工程化二期.md)
> 任务文档: [docs/tasks/2026-05-08-工程化二期-tasks.md](../tasks/2026-05-08-工程化二期-tasks.md)
> 日期: 2026-05-08
> 当前轮次: 第 1 轮（P0 核心，约 1000 行变更）
> Gate B 状态: 通过

---

## 1. 当前轮次目标

完成 TypeScript 全量迁移（REQ-001）、CI/CD 自动化流水线搭建（REQ-002）、Release 流程重写与 token/Gitee 清理（REQ-004），打通"类型安全编译 → 测试验证 → CI 自动化 → 安全发布"全链路。

---

## 2. 当前轮次范围

| 纳入 | 排除（留待第 2 轮） |
|------|---------------------|
| TASK-001 TS 基础设施搭建 | TASK-008 Agent Effort 统一 max→high |
| TASK-002 ESLint TypeScript 适配 | TASK-010 临时文件目录约束 |
| TASK-003 引擎核心模块 TS 迁移 | TASK-011 会话断联文档确认 |
| TASK-004 服务端+CLI+测试 TS 迁移 | TASK-012 版本发布流程文档化 |
| TASK-005 GitHub Actions CI 流水线 | |
| TASK-006 Husky Git Hooks | |
| TASK-007 Release 流程重写+脚本清理 | |
| TASK-009 .env Token 清理 | |

---

## 3. 完成标准

本轮的全局完成标准（所有任务全部通过）：

- [ ] `npm run typecheck` strict 模式下 **0 error**
- [ ] `npm run lint` **0 error**
- [ ] `npm run test` 全部 **18 测试通过**
- [ ] `npm run build` 成功编译产出 `dist/`
- [ ] `.github/workflows/ci.yml` 存在且语法正确
- [ ] `.husky/pre-commit` 和 `.husky/pre-push` 可用
- [ ] `scripts/sync-gitee-releases.js` 已删除
- [ ] `scripts/release.sh` 使用 `gh release create`，无 token/Gitee 引用
- [ ] `.env` 文件已删除
- [ ] `.env.example` 不含任何 token 占位符

---

## 4. 是否需要先查阅 code-explore-expert / docs-research-expert

**不需要。** 现有代码结构已通过需求文档和任务文档完整映射：
- 源文件列表及行数已在 TASK-003/004 中精确列出
- package.json 当前状态已读取确认
- AGENTS.md 发布流程章节（第 60-117 行）位置已定位
- 所有文件路径和当前状态已在规划前验证

---

## 5. 执行策略概述

### 串行链（最长路径）

```
TASK-001 (TS infra) → TASK-002 (ESLint TS) → TASK-003 (engine TS) → TASK-004 (server+CLI+tests TS)
```

这条链路是 TS 迁移的核心，严格串行，原因：
- TASK-002 需要 TASK-001 产出的 tsconfig.json + @typescript-eslint 已安装
- TASK-003 需要 TASK-002 的 ESLint 配置就位才能验证 lint 0 error
- TASK-004 导入 TASK-003 产出的模块，且需要 typecheck 全量通过

### 并行组

TS 迁移链路完成后，TASK-005/006/007/009 之间无共享文件冲突，可并行启动：

| 任务 | 涉及文件 | 冲突检查 |
|------|---------|---------|
| TASK-005 | `.github/workflows/ci.yml`（新建） | 独立 |
| TASK-006 | `.husky/`（新建），`package.json`（增量追加 lint-staged） | package.json 与 TASK-001 串行已解决 |
| TASK-007 | `scripts/`（修改/删除），`.github/workflows/release.yml`（修改） | 独立 |
| TASK-009 | `.env`（删除），`.env.example`（精简） | 独立 |

### 共享区域处理

| 文件 | 第一责任方 | 后续修改方 | 策略 |
|------|-----------|-----------|------|
| `package.json` | TASK-001（结构改动） | TASK-006（仅追加 lint-staged） | TASK-001 完成所有 scripts/bin/files/devDeps 改动后，TASK-006 仅增量追加单一配置块 |
| `AGENTS.md` | TASK-010（第 2 轮，追加约束） | TASK-012（第 2 轮，重写发布章节） | 严格串行 TASK-010 → TASK-012 |

---

## 6. 风险提醒

| 风险 | 级别 | 缓解措施 |
|------|------|---------|
| TASK-003 跨 5 个文件机械性改动，中间态 typecheck 可能报错 | 中 | 逐文件完成并验证，任一文件 typecheck 不过不进入下一文件 |
| TASK-004 跨 8 个文件，约 1600 行源码，变更规模大 | 中 | import 路径保持 `.js` 扩展名（ESM TypeScript 规范），测试仅变更路径不改变断言 |
| TASK-007 修改 release 流程，可能影响线上发布 | 中 | `sync-github-releases.js` 保留 `--dry-run` 模式；release.sh 先验证 `gh release create --help` 可用 |
| TASK-009 `.env` 含真实 token，已泄露到本地 | 高 | 删除前提醒用户手动轮换 GitHub token 和 npm token；确认不在 git 追踪中 |
| package.json 被两个任务修改（TASK-001 + TASK-006） | 低 | 严格串行，TASK-006 只做增量追加，不覆盖已有字段 |

---

## 7. parallel_batches

### Batch 1（无依赖，串行起始 — TS 基础设施搭建）

**依赖**: 无

| TASK | Subagent | 文件 |
|------|----------|------|
| TASK-001 | backend-dev-expert | `tsconfig.json`（新建），`package.json`（修改），`vitest.config.ts`（重命名），`jsconfig.json`（删除） |

**验证命令**:
```bash
# 确认 tsconfig.json 为 strict 模式
node -e "const c=require('./tsconfig.json'); console.log(c.compilerOptions.strict)"
# 确认 package.json scripts 更新
node -e "const p=require('./package.json'); console.log(p.scripts.typecheck, p.scripts.build, p.bin)"
# typecheck 可运行（无 .ts 文件时 0 error）
npm run typecheck
# build 可产出 dist/
npm run build && ls dist/
```

---

### Batch 2（依赖 Batch 1 — ESLint TypeScript 适配）

**依赖**: Batch 1 完成

| TASK | Subagent | 文件 |
|------|----------|------|
| TASK-002 | backend-dev-expert | `eslint.config.js`（修改） |

**验证命令**:
```bash
# ESLint 不再解析 .ts 文件报错
npm run lint
# 确认配置引入 @typescript-eslint
node -e "const m=require('@typescript-eslint/eslint-plugin'); console.log('OK')"
```

---

### Batch 3（依赖 Batch 2 — 引擎核心模块 TS 迁移）

**依赖**: Batch 2 完成

| TASK | Subagent | 文件 |
|------|----------|------|
| TASK-003 | backend-dev-expert | `src/engine/db.ts`，`src/engine/gates.ts`，`src/engine/agent-registry.ts`，`src/engine/agent-fs.ts`，`src/web/routes.ts`（均为 .js→.ts 重命名+类型标注） |

**验证命令**:
```bash
# TypeScript 严格检查通过（仅本轮迁移的文件）
npm run typecheck
# ESLint 对迁移文件 0 error
npm run lint
# 确认旧 .js 文件已删除
ls src/engine/*.js src/web/*.js 2>&1 | grep "No such" || echo "WARNING: old .js files still exist"
```

---

### Batch 4（依赖 Batch 3 — 服务端+CLI+测试 TS 迁移，TS 迁移终点）

**依赖**: Batch 3 完成

| TASK | Subagent | 文件 |
|------|----------|------|
| TASK-004 | backend-dev-expert | `src/engine/server.ts`，`src/cli.ts`，`src/install.ts`，`src/hook.ts`，`src/doctor.ts`，`tests/gates.test.ts`，`tests/db.test.ts`，`bin/jarvis.js`（修改 import 路径） |

**验证命令**:
```bash
# 全量 TypeScript 严格检查 0 error（强制执行）
npm run typecheck
# 全量 ESLint 0 error（强制执行）
npm run lint
# 全部 18 测试通过（强制执行）
npm run test
# build 成功产出 dist/
npm run build && ls dist/src/ dist/bin/
```

---

### Batch 5（依赖 Batch 4 — CI/CD + 清理并行组）

**依赖**: Batch 4 完成（TS 迁移全链路验证通过）

| TASK | Subagent | 文件 |
|------|----------|------|
| TASK-005 | backend-dev-expert | `.github/workflows/ci.yml`（新建） |
| TASK-006 | backend-dev-expert | `.husky/pre-commit`（新建），`.husky/pre-push`（新建），`package.json`（增量追加 lint-staged） |
| TASK-007 | backend-dev-expert | `scripts/release.sh`（重写），`scripts/sync-github-releases.js`（重写），`scripts/sync-gitee-releases.js`（删除），`.github/workflows/release.yml`（更新） |
| TASK-009 | backend-dev-expert | `.env`（删除），`.env.example`（精简） |

**验证命令**:
```bash
# TASK-005: 确认 CI YAML 文件存在且语法有效
ls .github/workflows/ci.yml && node -e "require('fs').readFileSync('.github/workflows/ci.yml','utf8')" | head -5

# TASK-006: 确认 husky hooks 存在
ls .husky/pre-commit .husky/pre-push
# 验证 lint-staged 配置在 package.json 中
node -e "const p=require('./package.json'); console.log(p['lint-staged'])"

# TASK-007: 确认 Gitee 脚本已删除
ls scripts/sync-gitee-releases.js 2>&1 | grep "No such" && echo "OK: deleted"
# 确认 release.sh 不引用 Gitee/Token
grep -i "gitee\|GITEE_TOKEN\|GITHUB_TOKEN\|NPM_TOKEN" scripts/release.sh && echo "WARNING" || echo "OK: clean"

# TASK-009: 确认 .env 已删除
ls .env 2>&1 | grep "No such" && echo "OK: deleted"
# 确认 .env.example 无 token
grep -i "TOKEN" .env.example && echo "WARNING" || echo "OK: clean"
```

---

## 8. Execution Packets

---

### TASK-001 Packet

```
### task_id: TASK-001
### task_name: TypeScript 基础设施搭建
### requirement_ids: REQ-001
### owner: backend-dev-expert
### objective: 搭建 TypeScript 编译基础设施，将项目从 JS+JSDoc 切换到 TypeScript 严格模式编译
### in_scope:
  - 删除 jsconfig.json，新建 tsconfig.json（strict: true, module: Preserve, outDir: dist）
  - 更新 package.json scripts（typecheck→tsc --noEmit, build→tsc, prepare→husky）
  - 更新 package.json bin 指向 dist/bin/jarvis.js
  - 更新 package.json files 为 ["dist/"]
  - 补充 devDependencies（typescript@^6.0, husky@^9, lint-staged@^15）
  - 重命名 vitest.config.js → vitest.config.ts，更新 include 匹配 .ts
  - 确认 types/punycode.d.ts 保留，tsconfig paths 引用正确
### out_of_scope:
  - 不改动任何 src/ 下的源码文件
  - 不改动 eslint.config.js（留给 TASK-002）
  - 不迁移任何 .js 文件到 .ts（留给 TASK-003/004）
  - 不处理 src/templates/ 下的模板文件（tsconfig exclude 已排除）
### input_documents:
  - docs/requirements/2026-05-08-工程化二期.md
  - docs/tasks/2026-05-08-工程化二期-tasks.md
### allowed_paths:
  - package.json
  - jsconfig.json（删除）
  - tsconfig.json（新建）
  - vitest.config.js → vitest.config.ts（重命名）
  - types/punycode.d.ts（只读确认，不改动）
### forbidden_paths:
  - src/（不迁移任何源码）
  - tests/（不迁移测试文件）
  - eslint.config.js（留给 TASK-002）
  - scripts/（留给 TASK-007）
  - .env / .env.example（留给 TASK-009）
  - AGENTS.md（留给 TASK-010/012）
### dependencies: 无
### required_skills:
  - behavioral-guidelines
  - code-standards
  - source-driven-development
  - incremental-implementation
  - verification-before-completion
### parallel_group: 无（串行链起点）
### wait_for: 无
### acceptance_criteria:
  - tsconfig.json 存在且 compilerOptions.strict === true
  - jsconfig.json 已删除
  - npm run typecheck 可运行（无 .ts 文件时 0 error）
  - npm run build 可产出 dist/ 目录
  - vitest.config.ts 配置正确（include 匹配 .ts）
  - npm install 无报错，新增 devDependencies 已安装
### test_strategy: manual_only
### handoff_notes:
  - package.json 的 scripts/bin/files/devDeps 由本任务完成所有结构性改动
  - TASK-006 将在 Batch 5 中增量追加 lint-staged 配置块，不覆盖已有字段
  - tsconfig.json 的 outDir: "dist" 决定了 TASK-004 中 bin/jarvis.js 的 import 路径
### escalation_rule: 如需变更共享契约、数据库结构、路由前缀、根配置，必须先回编排者，不得直接修改
```

---

### TASK-002 Packet

```
### task_id: TASK-002
### task_name: ESLint TypeScript 适配
### requirement_ids: REQ-001
### owner: backend-dev-expert
### objective: 将当前纯 JS 的 ESLint flat config 适配为支持 TypeScript，使 lint 能正确解析 .ts 文件
### in_scope:
  - 引入 typescript-eslint 的 config() helper
  - 将现有 js.configs.recommended 替换为 tseslint.configs.recommended
  - languageOptions.parser 设为 @typescript-eslint/parser
  - rules 中移除 JSDoc-only 规则，no-unused-vars 改为 @typescript-eslint/no-unused-vars
  - ignores 调整：移除 *.config.js 通配，添加 dist/
  - files 范围调整为 ['src/**/*.ts', 'tests/**/*.ts']
### out_of_scope:
  - 不修改 src/ 源码
  - 不修改 tsconfig.json
  - 不添加新的 ESLint 规则（仅做适配，非规则扩展）
### input_documents:
  - docs/requirements/2026-05-08-工程化二期.md
  - docs/tasks/2026-05-08-工程化二期-tasks.md
### allowed_paths:
  - eslint.config.js
### forbidden_paths:
  - src/（不迁移源码）
  - package.json（TASK-001 已完成）
  - tsconfig.json（TASK-001 已完成）
### dependencies: TASK-001 产出（tsconfig.json + @typescript-eslint devDependencies）
### required_skills:
  - behavioral-guidelines
  - code-standards
  - source-driven-development
  - incremental-implementation
  - verification-before-completion
### parallel_group: 无（串行链：在 TASK-001 之后，TASK-003 之前）
### wait_for: TASK-001
### acceptance_criteria:
  - ESLint 不再对 .ts 文件报解析错误
  - npm run lint 对现有 .ts 文件运行正常（即使暂时有 warn/error 也是可接受的）
  - eslint.config.js 中 files 范围为 .ts 文件
### test_strategy: manual_only
### handoff_notes:
  - 当前项目仍是 .js 文件，ESLint 跑在空的 .ts 文件集上应该 0 error
  - TASK-003 迁移后的 .ts 文件将通过此 ESLint 配置检查
### escalation_rule: 如需变更共享契约、数据库结构、路由前缀、根配置，必须先回编排者，不得直接修改
```

---

### TASK-003 Packet

```
### task_id: TASK-003
### task_name: 引擎核心模块 TS 迁移
### requirement_ids: REQ-001
### owner: backend-dev-expert
### objective: 将引擎核心模块（db, gates, agent-registry, agent-fs, routes）从 JS+JSDoc 迁移到 TypeScript，移除 JSDoc 类型注解并添加 TS 类型
### in_scope:
  - src/engine/db.js → src/engine/db.ts（重命名，JSDoc→TS 类型标注）
  - src/engine/gates.js → src/engine/gates.ts（重命名，添加 PIPELINE_DEFS 接口定义）
  - src/engine/agent-registry.js → src/engine/agent-registry.ts（重命名，添加 AgentInfo/AgentFileMap 接口）
  - src/engine/agent-fs.js → src/engine/agent-fs.ts（重命名，函数参数/返回值类型）
  - src/web/routes.js → src/web/routes.ts（重命名，Hono Context 类型标注）
  - 删除对应的 .js 文件
### out_of_scope:
  - 不变更业务逻辑（纯机械性类型标注迁移）
  - 不修改 server.ts / cli.ts 等 TASK-004 的文件
  - 不修改测试文件（留给 TASK-004）
### input_documents:
  - docs/requirements/2026-05-08-工程化二期.md
  - docs/tasks/2026-05-08-工程化二期-tasks.md
### allowed_paths:
  - src/engine/db.js → db.ts
  - src/engine/gates.js → gates.ts
  - src/engine/agent-registry.js → agent-registry.ts
  - src/engine/agent-fs.js → agent-fs.ts
  - src/web/routes.js → routes.ts
### forbidden_paths:
  - src/engine/server.js（留给 TASK-004）
  - src/cli.js / src/install.js / src/hook.js / src/doctor.js（留给 TASK-004）
  - tests/（留给 TASK-004）
  - package.json / tsconfig.json / eslint.config.js（前序任务已完成）
### dependencies: TASK-001（tsconfig.json + devDeps），TASK-002（ESLint TS 配置）
### required_skills:
  - behavioral-guidelines
  - code-standards
  - source-driven-development
  - incremental-implementation
  - verification-before-completion
### parallel_group: 无（串行链：在 TASK-002 之后，TASK-004 之前）
### wait_for: TASK-001, TASK-002
### acceptance_criteria:
  - 5 个 .js 文件删除，对应 .ts 文件存在
  - npm run typecheck 对这些文件 0 error
  - npm run lint 对这些文件 0 error
### test_strategy: test_after（迁移后运行测试确认断言不变；测试文件迁移在 TASK-004 中统一完成）
### handoff_notes:
  - import 路径保持 .js 扩展名（ESM TypeScript 规范），tsc 自动解析 .ts 源文件
  - 现有测试（tests/db.test.js, tests/gates.test.js）的 import 路径尚未适配，这些在 TASK-004 中处理
  - 关键类型接口（如 PIPELINE_DEFS, AgentInfo）需导出供 TASK-004 使用
### escalation_rule: 如需变更共享契约、数据库结构、路由前缀、根配置，必须先回编排者，不得直接修改
```

---

### TASK-004 Packet

```
### task_id: TASK-004
### task_name: 服务端+CLI+测试 TS 迁移
### requirement_ids: REQ-001
### owner: backend-dev-expert
### objective: 将服务端入口、CLI 入口、安装/钩子/健康检查及测试文件从 JS 迁移到 TypeScript，完成全量 TS 迁移
### in_scope:
  - src/engine/server.js → src/engine/server.ts（重命名，约 636 行）
  - src/cli.js → src/cli.ts（重命名，约 358 行）
  - src/install.js → src/install.ts（重命名，约 284 行）
  - src/hook.js → src/hook.ts（重命名，约 66 行）
  - src/doctor.js → src/doctor.ts（重命名，约 84 行）
  - tests/gates.test.js → tests/gates.test.ts（重命名，import 路径适配）
  - tests/db.test.js → tests/db.test.ts（重命名，import 路径适配）
  - bin/jarvis.js（更新 import 指向 dist/src/cli.js）
  - 删除对应的 .js 文件
### out_of_scope:
  - 不变更业务逻辑（纯机械性类型标注迁移）
  - 不修改 TASK-003 已迁移的 engine 核心模块
  - 不修改测试断言逻辑
### input_documents:
  - docs/requirements/2026-05-08-工程化二期.md
  - docs/tasks/2026-05-08-工程化二期-tasks.md
### allowed_paths:
  - src/engine/server.js → server.ts
  - src/cli.js → cli.ts
  - src/install.js → install.ts
  - src/hook.js → hook.ts
  - src/doctor.js → doctor.ts
  - tests/gates.test.js → gates.test.ts
  - tests/db.test.js → db.test.ts
  - bin/jarvis.js
### forbidden_paths:
  - src/engine/db.ts / gates.ts / agent-registry.ts / agent-fs.ts（TASK-003 已完成）
  - src/web/routes.ts（TASK-003 已完成）
  - package.json / tsconfig.json / eslint.config.js（前序任务已完成）
### dependencies: TASK-003（导入 engine/*.ts 和 web/routes.ts 模块）
### required_skills:
  - behavioral-guidelines
  - code-standards
  - source-driven-development
  - incremental-implementation
  - verification-before-completion
### parallel_group: 无（串行链终点）
### wait_for: TASK-003
### acceptance_criteria:
  - 所有 6 个源码 .js 文件删除，对应 .ts 文件存在
  - 2 个测试 .js 文件删除，对应 .ts 文件存在
  - bin/jarvis.js import 指向 dist/src/cli.js
  - npm run typecheck strict 模式下 **0 error**（全量通过）
  - npm run lint **0 error**
  - npm run test **全部 18 测试通过**
  - npm run build 可成功编译并产出 dist/ 目录
### test_strategy: test_after（迁移完成后运行全量 18 个测试验证断言无变化）
### handoff_notes:
  - ESM TypeScript 项目中 import 路径保持 .js 扩展名（如 import '../engine/db.js'），tsc 编译时自动解析 .ts 源文件
  - bin/jarvis.js 是编译后入口，指向 dist/src/cli.js（必须等 npm run build 通过后才能验证）
  - 这是 TS 迁移最后一步，完成后 pipeline 全链路可验证
### escalation_rule: 如需变更共享契约、数据库结构、路由前缀、根配置，必须先回编排者，不得直接修改
```

---

### TASK-005 Packet

```
### task_id: TASK-005
### task_name: GitHub Actions CI 流水线
### requirement_ids: REQ-002
### owner: backend-dev-expert
### objective: 创建 .github/workflows/ci.yml，实现 push→check（lint+typecheck+test）和 tag→build+publish 双 job 自动化
### in_scope:
  - 新建 .github/workflows/ci.yml
  - check job: push/PR 到 main 时自动运行 lint + typecheck + test（Node 22.x）
  - publish job: v* tag push 时 build + gh release create + npm publish（通过 NPM_TOKEN secret）
  - 包含 workflow_dispatch 手动触发
### out_of_scope:
  - 不修改 .github/workflows/release.yml（留给 TASK-007）
  - 不在 CI 中配置 Secret（NPM_TOKEN 需仓库管理员手动在 GitHub Settings 中添加）
### input_documents:
  - docs/requirements/2026-05-08-工程化二期.md
  - docs/tasks/2026-05-08-工程化二期-tasks.md
### allowed_paths:
  - .github/workflows/ci.yml（新建）
### forbidden_paths:
  - .github/workflows/release.yml（留给 TASK-007）
  - 任何 src/ / tests/ / package.json 文件
### dependencies: TASK-001（package.json 中 scripts 就位：lint/typecheck/test/build）
### required_skills:
  - behavioral-guidelines
  - code-standards
  - source-driven-development
  - incremental-implementation
  - verification-before-completion
### parallel_group: TASK-006, TASK-007, TASK-009
### wait_for: Batch 4（TS 迁移全链路验证通过）
### acceptance_criteria:
  - .github/workflows/ci.yml 文件存在
  - YAML 语法有效
  - check job 包含 lint / typecheck / test 步骤
  - publish job 包含 build / gh release create / npm publish 步骤
  - workflow 中无 token 硬编码
### test_strategy: manual_only（CI YAML 配置，需 push 后实际触发验证）
### handoff_notes:
  - 需要在 GitHub 仓库 Settings > Secrets and variables > Actions 中手动添加 NPM_TOKEN
  - check job 的 node-version 矩阵使用 [22.x]，与 engines.node >=22.5 一致
  - publish job 使用 github.token（自动提供）和 secrets.NPM_TOKEN
### escalation_rule: 如需变更共享契约、数据库结构、路由前缀、根配置，必须先回编排者，不得直接修改
```

---

### TASK-006 Packet

```
### task_id: TASK-006
### task_name: Husky Git Hooks
### requirement_ids: REQ-002
### owner: backend-dev-expert
### objective: 配置 Husky v9 + lint-staged，实现 pre-commit（lint-staged）和 pre-push（npm run check）两阶段检查
### in_scope:
  - 初始化 Husky（npx husky init）
  - 创建 .husky/pre-commit（npx lint-staged）
  - 创建 .husky/pre-push（npm run check）
  - 在 package.json 中增量追加 lint-staged 配置块（*.ts → eslint --fix + prettier --write）
### out_of_scope:
  - 不覆盖 TASK-001 写入的 package.json 任何已有字段
  - 不修改 tsconfig.json / eslint.config.js
### input_documents:
  - docs/requirements/2026-05-08-工程化二期.md
  - docs/tasks/2026-05-08-工程化二期-tasks.md
### allowed_paths:
  - .husky/（新建目录）
  - package.json（仅追加 lint-staged 配置块）
### forbidden_paths:
  - .github/（留给 TASK-005/007）
  - src/ / tests/
  - tsconfig.json / eslint.config.js
### dependencies: TASK-001（package.json 已有 prepare: "husky" + husky/lint-staged devDeps）
### required_skills:
  - behavioral-guidelines
  - code-standards
  - source-driven-development
  - incremental-implementation
  - verification-before-completion
### parallel_group: TASK-005, TASK-007, TASK-009
### wait_for: Batch 4（TS 迁移全链路验证通过）
### acceptance_criteria:
  - .husky/ 目录存在且包含 pre-commit 和 pre-push
  - git commit 时自动运行 lint-staged
  - git push 前自动运行 npm run check
  - package.json 中 lint-staged 配置存在且不为空
### test_strategy: manual_only（git hooks 本地验证）
### handoff_notes:
  - lint-staged 配置仅匹配 *.ts 文件，运行 eslint --fix 和 prettier --write
  - 如果项目不需要 prettier（当前 devDependencies 不含 prettier），可调整为仅 eslint --fix
  - Husky v9 使用 .husky/ 目录而非 package.json 的 husky 字段
### escalation_rule: 如需变更共享契约、数据库结构、路由前缀、根配置，必须先回编排者，不得直接修改
```

---

### TASK-007 Packet

```
### task_id: TASK-007
### task_name: Release 流程重写+脚本清理
### requirement_ids: REQ-002, REQ-004
### owner: backend-dev-expert
### objective: 重写 release 脚本脱离 Gitee/token 依赖，删除 Gitee 同步脚本，更新 GitHub Actions release workflow
### in_scope:
  - scripts/release.sh 重写：删除 Gitee Release + GitHub Release curl 步骤，改为 gh release create
  - scripts/release.sh 移除 NPM_TOKEN 环境变量检查（本地依赖 ~/.npmrc）
  - scripts/sync-github-releases.js 重写：用 gh release list + gh release create CLI
  - scripts/sync-gitee-releases.js 删除
  - .github/workflows/release.yml 更新：移除 Gitee URL，changelog 改 GitHub URL
### out_of_scope:
  - 不修改 .github/workflows/ci.yml（留给 TASK-005）
  - 不实际执行发布（仅脚本层面修改）
### input_documents:
  - docs/requirements/2026-05-08-工程化二期.md
  - docs/tasks/2026-05-08-工程化二期-tasks.md
### allowed_paths:
  - scripts/release.sh（重写）
  - scripts/sync-github-releases.js（重写）
  - scripts/sync-gitee-releases.js（删除）
  - .github/workflows/release.yml（更新）
### forbidden_paths:
  - .github/workflows/ci.yml（留给 TASK-005）
  - src/ / tests/ / package.json
### dependencies: TASK-001（package.json 版本号结构已确定）
### required_skills:
  - behavioral-guidelines
  - code-standards
  - source-driven-development
  - incremental-implementation
  - verification-before-completion
### parallel_group: TASK-005, TASK-006, TASK-009
### wait_for: Batch 4（TS 迁移全链路验证通过）
### acceptance_criteria:
  - scripts/sync-gitee-releases.js 文件不存在
  - scripts/release.sh 使用 gh release create，不含 GITEE_TOKEN / GITHUB_TOKEN / NPM_TOKEN 引用
  - scripts/sync-github-releases.js 使用 gh CLI 命令
  - .github/workflows/release.yml 引用 GitHub URL（非 Gitee URL）
  - grep -i "gitee" scripts/ 无匹配
### test_strategy: manual_only（发布脚本需实际发布验证，建议 dry-run 测试）
### handoff_notes:
  - sync-github-releases.js 保留 --dry-run 模式便于测试
  - release.sh 的三个步骤：更新版本号 → git tag + push → npm publish（仅这 3 步）
  - 发布验证命令已包含 npm view + git ls-remote tags
### escalation_rule: 如需变更共享契约、数据库结构、路由前缀、根配置，必须先回编排者，不得直接修改
```

---

### TASK-009 Packet

```
### task_id: TASK-009
### task_name: .env Token 清理
### requirement_ids: REQ-004
### owner: backend-dev-expert
### objective: 删除含真实 token 的 .env 文件，清理 .env.example 中所有 token 占位符段落
### in_scope:
  - 删除 .env 文件
  - 精简 .env.example：仅保留 # PORT=3456 注释行，删除所有 token 相关段落
  - 确认 .gitignore 已排除 .env（当前已包含，无需额外操作）
### out_of_scope:
  - 不在 GitHub 上撤销已泄露的 token（手动安全操作，提醒用户完成）
  - 不修改其他 .env.* 文件
### input_documents:
  - docs/requirements/2026-05-08-工程化二期.md
  - docs/tasks/2026-05-08-工程化二期-tasks.md
### allowed_paths:
  - .env（删除）
  - .env.example（修改）
### forbidden_paths:
  - .gitignore（当前规则已正确，只读确认即可）
  - 任何 src/ / tests/ / scripts/ 文件
### dependencies: 无
### required_skills:
  - behavioral-guidelines
  - code-standards
  - source-driven-development
  - verification-before-completion
### parallel_group: TASK-005, TASK-006, TASK-007
### wait_for: Batch 4（TS 迁移全链路验证通过）
### acceptance_criteria:
  - .env 文件不存在
  - .env.example 仅含 PORT= 注释，无 token 相关行
  - grep "GITEE_TOKEN\|GITHUB_TOKEN\|NPM_TOKEN" .env.example 无匹配
### test_strategy: manual_only（grep 验证）
### handoff_notes:
  - **重要**: 提醒用户在 GitHub 个人设置中撤销已泄露的 token 并重新生成
  - .env.example 当前可能包含多行 token 占位符，全部删除
  - .gitignore 第 6 行已有 .env 规则，第 7 行有 .env.* 规则，第 8 行允许 .env.example，无需修改
### escalation_rule: 如需变更共享契约、数据库结构、路由前缀、根配置，必须先回编排者，不得直接修改
```

---

## 9. plan patch / contract change request 触发条件

以下任何情况发生时，实现代理必须暂停并通过编排者提 plan patch：

| 触发条件 | 影响范围 | 处理方式 |
|---------|---------|---------|
| tsconfig.json 的 strict 模式导致无法解决的编译错误 | 延迟整个 TS 迁移链路 | 回编排者评估是否放宽个别 strict 子选项 |
| import 路径适配发现循环依赖 | 影响 TASK-003/004 的模块导入 | 回编排者重新评估模块拆分 |
| 现有 18 个测试在 TS 迁移后断言失败（非路径问题） | 表明类型标注引入了行为变更 | 回编排者评估是否需要修改测试或类型定义 |
| package.json 的 bin/files 变更后 CLI 无法启动 | 影响全局安装用户 | 回编排者确定正确的编译入口路径 |
| Husky init 在 Windows 环境失败 | 影响 TASK-006 | 回编排者评估替代方案或平台兼容策略 |
| scripts/release.sh 的 gh CLI 在本地不可用 | 影响 TASK-007 验证 | 回编排者决策安装 gh 还是保留 curl 回退 |
| .env 发现额外敏感信息 | 影响 TASK-009 安全清理范围 | 回编排者扩大清理范围 |

---

## 10. 实现者交接信息

### 给 backend-dev-expert 的统一指引

1. **本项目是纯 Node.js ESM 后端 CLI 项目**（TypeScript + Hono + Vitest + ESLint）
2. **所有任务均为直接开发（无 TDD/DDD）**，验证方式为运行 lint/typecheck/test/build 四件套
3. **import 路径约定**: ESM TypeScript 中 import 保持 `.js` 扩展名，tsc 会自动解析对应 `.ts` 源文件
4. **tsconfig exclude**: `src/templates/` 目录下的模板文件（含 opencode node_modules）不在 TS 编译范围
5. **每个任务完成前必须运行验证命令**，将输出贴在 Execution Acknowledgement 中
6. **行为准则**: 遵守 behavioral-guidelines 四项准则——先思考再编码、简单优先、精准修改、目标驱动

### 关键版本号

| 包 | 当前版本 | 要求 |
|----|---------|------|
| typescript | ^6.0.3 | 严格模式编译 |
| vitest | ^4.1.5 | 测试框架 |
| eslint | ^10.3.0 | 需搭配 @typescript-eslint |
| hono | ^4.12.17 | Web 框架 |
| @modelcontextprotocol/sdk | ^1.29.0 | MCP SDK |
| Node.js | >=22.5 | 运行时 |

---

## 11. 推荐的下一步

1. **立即执行**: 启动 Batch 1 → TASK-001（backend-dev-expert），TS 基础设施是全部后续任务的前置
2. **第 1 轮完成后**: 启动第 2 轮（TASK-008 ∥ TASK-011，然后 TASK-010 → TASK-012，约 170 行）
3. **安全操作**: TASK-009 执行前，确认用户已在 GitHub/npm 中轮换了已泄露的 token
4. **CI 验证**: TASK-005 的 CI YAML 需 push 到 GitHub 后才能在 Actions 中实际触发验证
5. **最终 Gate 检查**: 全部 8 个任务完成后，由 qa-review-expert 做最终代码审查

---

## 12. 变更规模估算

| Batch | 任务 | 预估变更行数 |
|-------|------|-------------|
| 1 | TASK-001 | ~100 |
| 2 | TASK-002 | ~50 |
| 3 | TASK-003 | ~200 |
| 4 | TASK-004 | ~300 |
| 5 | TASK-005 + TASK-006 + TASK-007 + TASK-009 | ~120 + ~50 + ~150 + ~30 = ~350 |
| **合计** | | **~1000 行** |

在单轮次 ~1000 行变更预算内，符合变更规模控制要求。

---

## 附录 A：文件冲突矩阵

```
                  package.json  eslint.config  tsconfig  vitest.config  src/engine/*  src/web/*  tests/*  scripts/*  .github/*  .husky/*  .env*  AGENTS.md
TASK-001           W             -              W         W              -             -          -        -          -          -         -      -
TASK-002           -             W              -         -              -             -          -        -          -          -         -      -
TASK-003           -             -              -         -              W             W          -        -          -          -         -      -
TASK-004           -             -              -         -              W(server)     -          W        -          -          -         -      -
TASK-005           -             -              -         -              -             -          -        -          W(ci.yml)  -         -      -
TASK-006           R(append)     -              -         -              -             -          -        -          -          W         -      -
TASK-007           -             -              -         -              -             -          -        W          W(release) -         -      -
TASK-009           -             -              -         -              -             -          -        -          -          -         W      -
```

> W = Write（写入）, R = Read then append（读取后增量追加）, - = 不涉及

## 附录 B：串行/并行依赖图

```
Round 1:
  Batch 1: TASK-001
              │
  Batch 2: TASK-002
              │
  Batch 3: TASK-003
              │
  Batch 4: TASK-004
              │
  Batch 5: TASK-005 ∥ TASK-006 ∥ TASK-007 ∥ TASK-009

Round 2 (next):
  TASK-008 ∥ TASK-011
  TASK-010 → TASK-012
```
