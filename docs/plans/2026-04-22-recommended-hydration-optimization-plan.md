# 2026-04-22 推荐流水合优化计划

## 执行顺序
1. 通过测试锁定“推荐流先排后水合”的行为目标。
2. 重构 `postsService.listFeed` 的推荐分支，先基于轻量数据排序，再只水合当前页命中项。
3. 保持现有推荐排序、多样性、分页输出不变。
4. 跑相关测试，再跑完整 `lint / typecheck / test / build`。
5. 更新 review 文档。

## 风险控制
- 不改 API 契约。
- 不引入新的共享包依赖。
- 不在这一轮顺手扩展到 `latest/following` 流，聚焦推荐流。
