# Jarvis Agent Factory

> 所有智能体启动时必须读取 **[AGENTS.md](./AGENTS.md)** — 项目约束、架构约定、发布流程均在其中。

> **session_join 约定**：调用 `session_join` 时务必传入 `task_name` 参数，值为用户任务的一句话摘要（如"G6→X6改造：每Gate独立Agent图"），确保 Web 面板会话列表显示有意义的标题。
