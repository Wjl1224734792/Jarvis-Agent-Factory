{
  "name": "feijia",
  "private": true,
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "bun run --cwd apps/web dev",
    "dev:admin": "bun run --cwd apps/admin dev",
    "dev:server": "bun run --cwd apps/server dev",
    "db:generate": "bun run --cwd packages/db generate",
    "db:migrate": "bun run --cwd packages/db migrate",
    "db:seed": "bun run --cwd packages/db seed",
    "typecheck": "bun run --cwd packages/shared typecheck && bun run --cwd packages/schemas typecheck && bun run --cwd packages/http-client typecheck && bun run --cwd packages/db typecheck && bun run --cwd apps/server typecheck && bun run --cwd apps/web typecheck && bun run --cwd apps/admin typecheck",
    "test": "vitest run --config ./vitest.config.ts packages/schemas/tests/**/*.test.ts packages/http-client/tests/**/*.test.ts apps/web/tests/**/*.test.ts apps/admin/tests/**/*.test.ts && bun run --cwd apps/server test",
    "build": "bun run --cwd packages/shared build && bun run --cwd packages/schemas build && bun run --cwd packages/http-client build && bun run --cwd packages/db build && bun run --cwd apps/server build && bun run --cwd apps/web build && bun run --cwd apps/admin build",
    "check": "bun run test && bun run typecheck && bun run build"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@types/node": "^25.5.0",
    "@types/pg": "^8.20.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "drizzle-kit": "^0.31.10",
    "tailwindcss": "^4.2.2",
    "typescript": "^6.0.2",
    "vite": "^8.0.3",
    "vitest": "^4.1.1"
  }
}
